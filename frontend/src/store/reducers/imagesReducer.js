import {GET_PLACES_REQUEST} from "../actions/actionsTypes";

const INITIAL_STATE = {
    images: null,
    error: null
}

export default function (state = INITIAL_STATE, action) {
    switch (action.type) {
        default: return state
    }
}